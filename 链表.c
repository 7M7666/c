#include <stdio.h>
 typedef struct node
{
    int value;
    struct node *next
}Node;
int main()
{
    Node *head=NULL;
    int number;
    do{
       scanf("%d",&number);
       if(number!=-1)
       {

       } 
    }while(number!=-1);
    return 0;
}